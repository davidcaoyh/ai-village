# Decision log

One entry per non-obvious choice, in the format **decision -> alternatives -> why**.
Entries are written when the choice is made, not reconstructed at the end.
Several carry numbers measured on live calls; those are dated.

| # | Decision | Alternatives considered | Why |
|---|---|---|---|
| D1 | Reach all models through OpenRouter (one OpenAI-compatible API) | native SDK per provider; LiteLLM proxy | One client, one key, one spend dashboard; swapping a model is a config edit. ~5.5% fee is irrelevant at this scale. Isolated in `village/llm.py`, so reverting is a one-file change. |
| D2 | No agent framework; a hand-written ~150-line loop | LangGraph, CrewAI, AutoGen | The loop is small, and writing it is exactly the skill being screened for. When an agent misbehaves you debug your own code, not a framework's abstraction. Frameworks win on large teams and complex graph workflows; neither applies here. |
| D3 | Turn-based round-robin scheduling | true async concurrency | Reproducible bugs, readable conversation order, one budget knob. The real village is concurrent - listed as future work. |
| D4 | Event-sourced SQLite; one append-only `events` table is the only state | ORM with per-entity tables; Postgres | UI, replay, debugging, and analysis all become queries over one table; nothing can drift out of sync. SQLite = zero ops, one file to back up. |
| D5 | FastAPI + WebSocket + single vanilla-JS page | React + build step; HTTP polling; SSE | The frontend is a log viewer. A build pipeline buys nothing and costs POC hours. SSE would also have been fine. |
| D6 | Memory = rolling window + agent-written notes + periodic self-compaction | full history in prompt; vector-DB retrieval | Prompts cannot grow forever - cost rises and quality falls. Compaction is the same pattern Claude Code uses. Retrieval is the natural v3. |
| D7 | Safety rails from day 1: no shell tool, untrusted-data framing on fetched content, no contacting real people, spend + turn caps | add them after the demo works | The real village hallucinated a 93-person contact list and sent ~300 spam emails. Anticipating those is the difference between a toy and a system. |
| D8 | Cast defined entirely in `configs/agents.yaml` | agents constructed in Python | Adding a villager or running a hero session on bigger models is a config edit. Also makes runs comparable: one thing changes at a time. |
| D9 | Cost read from `usage.cost` on the response; no local price table | hardcoded per-model $/token table; recompute from `/models` pricing | OpenRouter returns `cost` (USD) on **every** response with no request flag (the old `usage: {include: true}` is deprecated and ignored). A local table silently drifts the day a provider reprices; the provider's own number cannot. Verified Aug 16, 2026 on a live call: `usage.cost = 0.0001229`. |
| D10 | Reasoning **suppressed** per agent (`reasoning: {effort: ...}` in `agents.yaml`); `reasoning_details` **not echoed** across turns | echo `reasoning_details` verbatim (OpenRouter's recommendation for multi-turn tool calling); leave reasoning at provider default | Measured on `openai/gpt-5-nano`: 256 of 299 completion tokens — **~83% of the call's cost** — went to hidden reasoning before emitting a 12-word tool call. Villagers select one action from a 7-tool menu; that is not reasoning-hard work. Dropping also helps correctness: an agent acts once per turn against a world that changed since it last spoke, so resuming stale reasoning is a liability, not continuity. And 4 models from 4 families emit this field in 4 shapes — echoing means 4 code paths through the hottest function. **Counterpoint:** on a genuinely multi-step chain (search → read → decide), echoing measurably improves coherence, and that is the version to build for a research-grade run. `llm.py` still *returns* `reasoning_details`, so the policy lives in `agent.py` and flipping it is a few lines. Note `exclude: true` only *hides* reasoning — you are still billed; only lowering `effort` reduces tokens. |
| D11 | Strip reasoning blobs from `raw` before persisting to the event log | log the provider payload verbatim | `reasoning_details[].data` is an encrypted chain-of-thought thousands of characters long. Measured on one real turn: stripping took the logged payload from 6,244 → 1,999 bytes (**−68%**). Over ~480 turns that dominates the SQLite file and slows every replay query (D4). The human-readable `reasoning.summary` is kept (truncated to 2,000 chars) because it genuinely helps when debugging why an agent chose an action. |
| D12 | Search backend: Tavily when `TAVILY_API_KEY` is set, DuckDuckGo (`ddgs`) otherwise | Tavily only; DuckDuckGo only; a paid SERP API | The fallback means anyone can clone the repo and run it with zero accounts - which matters when the person evaluating you tries it. Tavily is preferred when present because its snippets are written for LLM consumption and are markedly cleaner, which directly reduces tokens. |
| D13 | `tools.execute()` never raises; every failure returns an observation string | let exceptions propagate and fix the prompt | A 404 at turn 60 would kill a session you paid for, and the agent would very likely have recovered on its own. This is the same transport-failure vs. model-mistake distinction `llm.py` already draws, applied one layer up. Arguments are bound with `inspect.signature().bind()` *before* the call so a missing key is named rather than surfacing as a `TypeError` from inside a tool. |
| D14 | `end_turn` signals via a flag on `ToolContext`, not an exception | raise `TurnEnded`; return a sentinel string the loop string-matches | An agent choosing to stop is the normal end of a turn, not an error - control flow that reads as an error path invites someone to log it as one. And `execute()` must never raise (D13), so an exception would be the one inconsistency in the module. |
| D15 | The assistant message is **reconstructed** from normalized fields before being appended to the conversation | echo `resp.raw["choices"][0]["message"]` back verbatim | The raw message carries provider-specific extras (reasoning blobs, the `reasoning_summary` we add in D11) that some providers reject on the way back in. Rebuilding from `{id, name, arguments}` keeps the conversation provider-neutral, which is the entire promise of `llm.py`. Cost of getting this wrong: an intermittent 400 that only appears on multi-step turns. |
| D16 | Each turn's prompt is **rebuilt from the event log**, not carried as a growing message list | keep a persistent per-agent message list in memory | Three things fall out for free: a crashed process resumes exactly where it stopped, every turn's context is reproducible from the database, and the window can never quietly grow past its bound. Costs a little assembly work per turn. |
| D17 | Events are **rendered as prose lines** into the prompt, not dumped as JSON | JSON-serialize the event rows | A model reads `[claude] we should split the sources` far more reliably than a nested object, at roughly a third of the tokens. `memory._describe()` is the single place that decides what history looks like to an agent - the first place to tune when behaviour disappoints. |
| D18 | A turn is bounded by `max_steps` (default 6); no tool call twice ends the turn | let a turn run until `end_turn` | An agent will occasionally loop on a failing tool. Bounding a turn keeps one confused villager from eating the whole session budget. Hitting the cap is logged (`step_cap_reached`) rather than silently swallowed, because a villager that regularly hits it is a *prompt* problem, not a code problem. |
| D19 | Independent stop conditions, all exiting through one `finally` that writes `session_end`. Four at first (turn cap, spend guard, human kill switch, KeyboardInterrupt), six after D32 added `provider_unavailable` and `crashed`, eight after D36 added `goal_met` and `stalled` | rely on the turn cap alone | An autonomous loop that spends money needs more than one way to end, because the failure you did not anticipate is the one that runs all night. Single exit path means a session always ends with a `session_end` event: a session that merely stops emitting events is indistinguishable from a crash six weeks later. The kill switch takes effect at a turn boundary, not mid-turn, so the log never contains a tool call with no result. |
| D20 | The kill switch is stored as an **event**, not a flag column or a signal | a `sessions` table with a `stopped` boolean; a Unix signal | "Who stopped this session, and when" becomes answerable from the same log as everything else. One source of truth means one place to look (D4). |
| D21 | The web server **cannot start a session** - two processes, one SQLite file | one process serving HTTP and running the village | A crashed browser tab, a reload, or a uvicorn restart cannot disturb a paid-for run, and a viewer can attach to a session that is already three hours old. WAL mode lets the server read while the orchestrator writes. |
| D22 | The socket **polls** SQLite every 0.7s rather than being notified | a queue/pubsub between orchestrator and server | Two separate processes would need real infrastructure to pass notifications; polling an indexed `(session_id, id)` read is cheap. Right at four agents, wrong at four hundred. |
| D23 | Human messages are `system` events with `kind=human_message`, and rate-limited to one per 20s | append them as `chat` events from a fake agent | The log must never blur what a *model* said with what a *person* said - six weeks later that distinction is the whole dataset. The rate limit exists because a viewer who can type continuously turns the village into a chat toy and the transcript stops being evidence of anything. |
| D24 | Tests drive the loop through an injected `chat_fn`, so the whole suite runs offline | integration tests against the live API | A test suite that needs an API key is a test suite you stop running, and an agent system whose loop can only be exercised by spending money is one you cannot refactor. 70 tests, no network, ~1s. |
| D25 | A tool logs an event **only** when it produces an event type the agent loop cannot: `send_chat` -> `chat`, `end_turn` -> `system`. Nothing else calls `ctx.log` | let every tool mirror its own effect into the log | `agent.py` already appends one `action` before `execute()` and one `result` after it, with the identifying arguments. Tools that logged again put every action into the log twice, and the rolling window (D6) then re-sent both copies to every villager on every turn - a duplicate you pay for once per turn for the rest of the session. Found by reading a `--fake` transcript, which is what `--fake` is for. |
| D26 | An agent's own past tool result renders as the first 240 chars of the observation | render a bare count (`n`, `chars`); render the full observation | A count is not actionable: `(result of your web_search: 5)` on turn 40 tells the agent nothing, so it re-runs the same search and pays twice. The full observation is unbounded and would defeat the window. 240 characters carries the first URL or the error message, and is bounded - the only property that matters inside a fixed window. |
| D27 | The offline cast is a committed module (`village/fake.py`, `--fake`), not a throwaway script | a `FAKE_MODE` branch inside `llm.py`; a scratch file that is not committed | Everything except the provider call is identical in a fake run, so the UI, the log, the scheduler and the deploy can all be demonstrated and debugged with no key, no balance and no network. The rejected alternative puts test-only code on the code path that spends real money, which is the last place to be clever. A test asserts the fake's signature still matches `llm.chat`, because the moment they drift an offline run stops proving anything about a live one. |
| D28 | `POST /api/stop` requires `X-Village-Token` once `VILLAGE_ADMIN_TOKEN` is set; `POST /api/message` stays open to anyone | leave both open (as on localhost); put both behind auth; drop the kill switch from the UI | The two write endpoints are not the same kind of thing. A spectator message is *content* the village is free to ignore - and it is the exhibit, so gating it would remove the reason to visit. Stopping is *control* over a run that costs money and cannot be resumed, so on a public URL an anonymous stop button is a griefing vector, not a feature. The token is empty by default, which is right on a laptop and wrong on a host - so `deploy/village-web.service` always sets one. |
| D29 | Deployed as two systemd units on one small VPS (`village-web` always on, `village-session` oneshot on a weekly timer) behind Caddy | Docker Compose; a PaaS (Fly/Render/Railway); one process doing both | The artifact is one Python process plus one SQLite file. Docker would add a build step, an image registry and a volume mount to arrive at the same place. A PaaS charges more for an always-on instance and makes a persistent local file the awkward case rather than the normal one. Oneshot-plus-timer, rather than a daemon, because a session is a *bounded job* - it has a turn cap and a spend cap and ending is the expected outcome. `Restart=always` on that unit would be a machine that spends money forever. Caddy over nginx because it renews TLS itself and proxies WebSockets with no extra directives. |
| D30 | A Tavily failure falls through to DuckDuckGo; a `TAVILY_API_KEY` still holding the `.env.example` placeholder counts as unset | let the Tavily error reach the agent as an observation; require a valid key | The failure this prevents is specific and was real in this repo: an unedited `TAVILY_API_KEY=tvly-...` is *truthy*, so the keyed path is taken, every call 401s, and the keyless fallback that exists precisely for this case never runs - silently, for an entire session, while the agents conclude the web has nothing on their question. A backend choice is an implementation detail; the agent asked for search results. DuckDuckGo keeps failing loudly, because it is the last resort and has nowhere to fall through to. `scripts/preflight.py` deliberately tests the key *directly* rather than through the tool, so the fallback cannot hide a dead key from a preflight. |
| D31 | A compaction is a private `compaction` event that **supersedes** every note written before it; `notes_for()` returns that text plus the notes written since | a separate `notes` table with a summary column; mutate or delete the prior `write_note` events; keep appending and truncate the oldest | Keeps D4's promise that the event log is the only state: nothing is edited, nothing is deleted, and the full pre-compaction history is still there to replay. It is also what makes the notes section of the prompt *bounded* rather than merely capped - a cap at 8 notes silently loses turn 3's decision at turn 40, whereas a compaction carries it forward in compressed form. Private, like `thought`: a summary written for one agent is not a message to the others, and putting it in their window would be paying for a second, worse copy of the chat they already saw. The call is a plain completion (`tools=[]`) because it must return text, not an action - a model given tools here reliably calls `write_note` instead of answering. Failure is logged as `compaction_failed` and the turn proceeds (D13's rule one layer up): a lost summary is a worse prompt, not a dead session. The call also writes a `thought` event with no prose, so `session_cost` and the eval queries count the money it spends. **Cost:** fidelity. Anything the agent's own summary drops is gone from its memory, even though the events are still in the log - which is exactly the trade retrieval would buy back (see future work). |
| D32 | A provider failure ends the **turn**, not the session: `take_turn` catches `LLMError`, logs `provider_error` and hands over. A full round in which every villager failed ends the session as `provider_unavailable`. Any other unhandled exception is logged as `crashed` and re-raised | let `LLMError` propagate (the original behaviour); retry inside the loop until it succeeds; abort on the first provider error | Measured Sep 1, 2026 on the first deployed run: a 402 at turn 10 of 120 killed the process, and because `reason` was initialised to `"turn_cap"` before the `try` and only reassigned in the `BudgetExceeded` and `KeyboardInterrupt` handlers, the `finally` recorded the crash as a clean finish. D13 already says a tool failure must not kill a paid run; the model call is the only step that actually spends money and had no such guard, while `Agent.compact` (D31) already had one. Stopping after a full failed round rather than on the first error keeps one flaky provider from ending a session, and keeps a dead key from writing 110 turns of nothing. Re-raising preserves a non-zero exit so systemd still marks the unit failed. |
| D33 | Retry classification reads the error **body**, not just the HTTP status: 402 retries only when the payload carries `in_flight_budget_exhausted` | keep the status-code-only rule; make 402 retryable outright; retry everything a fixed number of times | The Sep 1 outage was a 402 whose own message read "Retry after in-flight requests settle, or add credits" - the same status code OpenRouter returns for a genuinely empty account. `llm.py` claimed "400/401/402/404 are permanent... waiting never fixes any of those", which is right for the second case and wrong for the first. Blanket-retrying 402 would sleep three times on every call for the rest of a session once the balance really is zero, so the discriminator has to be the metadata. The cost is one provider-specific string in `llm.py`, which is the module whose job is to know about the provider. |
| D34 | The action space gains `read_file`, `list_files` and `edit_file`; `edit_file` replaces one `## ` section and is the preferred way to change a file. `read_notes` is dropped from the default cast | keep write-only files and let agents relay contents through chat; give agents a shell; a full patch/diff format | Measured Sep 2, 2026 on a 72-turn live run: with `write_file` and no reader, **15 of 34 chat messages were agents asking each other to paste `brief.md`**, and the file was rewritten whole **30 times**, swinging between 49 and 4,150 characters and never converging - one write erased it. Whole-file rewriting also collided with `max_tokens`, truncating the document mid-sentence on 18 calls. Section editing fixes both at once: an agent spends ~200 tokens instead of 900, and cannot delete a section it never looked at. A diff format was rejected as more syntax for a model to get wrong; headings already exist in the document. `read_file` wraps what it returns in a `<village_file>` envelope for the same reason `fetch_url` does: a villager can paste web text into a shared file, so the label has to travel with the text. A miss names the files that do exist, and `fetch_url` now names `read_file` in its error, because the cast burned whole turns guessing at `file://` urls when the only tool whose description said "read" was the one for web pages. `read_notes` went because `build_context` already puts notes in the system prompt - the tool burned 32 calls re-reading visible text, 15 of them on the most expensive villager. |
| D35 | Every turn writes exactly one `turn_end`, carrying `ended_by` (`end_turn` / `step_cap` / `no_tool_call` / `provider_error`); the `end_turn` tool no longer logs it | count turns from `end_turn` only (the original); count them in the orchestrator and not in the log | Counting off the tool undercounted by 40%: the Sep 2 run recorded 43 `turn_end` events for **72 turns actually taken**, because a turn that hits the step cap or never acts calls nothing. Every per-turn number in M4 would have been inflated by the same 40%. Putting the event in the loop rather than the tool also makes `ended_by` a free per-model quality metric - DeepSeek ended 18 turns on the step cap, Gemini abandoned 5. This bends D25 (a tool logs only what the loop cannot): the loop can now see every ending, so it owns the event. |
| D36 | A season ends when the goal is met, decided by the villagers: `vote_done` records one villager's judgement, and the goal ends only when **every** villager holds a vote that nothing has overtaken. Any `write_file` or `edit_file` cancels every standing vote; chat and searches do not. Behind it, `max_idle_rounds` (default 3) ends the goal as `stalled` when that many full rounds pass with no action but talking and reading. `goals:` in season.yaml is a queue, and with `repeat: true` a met goal always opens another round on a fresh file (`brief.md`, `brief-2.md`, ...) rather than ending the session, so one goal text runs the whole budget. A met goal writes `goal_advanced` and the village continues | run to the turn cap (the original); a single agent may declare victory; an LLM judge polled by the orchestrator; stall detection alone; only ever set goals that cannot be completed | Measured Sep 1, 2026 on the first full 120-turn run: 33 of 35 edits landed in the first 40 turns, and the remaining 82 turns - **68% of the session, ~$0.49 of $0.73** - produced 2 edits and 67 chat messages, 87% of them a variant of "I re-read it, it is complete". Nothing malfunctioned: the season said produce a brief and never said what done meant, the turn count guaranteed 120 turns, and the prompt's "do not call end_turn without having done anything" made inventing verification work the obedient move. A specification bug. **Unanimity** because the transcript shows how eager one agent is to declare victory - one vote is an opinion, agreement is a fact about the group. **Writes cancel votes, chat does not**, because 67 messages agreeing it was finished must not keep a village alive; `edit_file` counts because 34 of the 35 changes in that run were section edits, so watching `write_file` alone would let a vote survive a rewritten section. The **backstop** exists because a village can be done and refuse to say so, and it cannot tell that from looping - which is fine, both should stop. `read_file` and `list_files` are exempt from it: the observed idle loop *was* re-reading the finished brief. An **LLM judge** was rejected as a hidden fifth voice with authority over the village and a cost per check, when the villagers already hold the information and consensus keeps the decision in the transcript where a spectator can watch it happen. The round number is derived from `goal_advanced` events rather than held in a variable, so a crashed session resumes on the right round - D16's trick again. A repeating season is also the shape the real AI Village runs: it is time-boxed rather than task-boxed, with the operators posting a fresh goal each Monday. Here the completion signal chooses the moment and the turn cap still owns the budget, so `goal_met` ends a *round* and the session ends on the cap - which makes the cap an honest reason again rather than a description of 82 idle turns. |
| D37 | One run is one `session_id`, and the page shows exactly one run: the header's picker is built from `GET /api/sessions` and defaults to the newest, so older runs stay reachable as an archive. Every feed - the four agent columns and the chat - trims to `FEED_MAX` (200) nodes | one endless page across all runs; paginate `/api/events`; a separate archive page; no picker, reach an old run by hand-editing `?session=` | Asked Sep 2 whether three weeks of daily runs would crash the front end. It would not, and the reason is the design rather than any guard: a session is turn-capped, so a measured 120-turn fake run is 1,570 events and 96 chat messages on day 1 and the same on day 21, comfortably under the 5,000 `/api/events` limit. The accumulation lands in SQLite, which is what an append-only log is for. What was actually missing was the *reverse* of the feared problem: `/api/sessions` existed and nothing called it, so 21 days of work would have had a homepage showing only today and no way to reach yesterday except by typing a session id into the url. Switching does a full page load rather than a client-side swap, because a run is the unit of everything on the page - cost, goal, artifacts, websocket cursor - and reloading is three lines where tearing all of that down is thirty. The trim is not needed at today's numbers; it is there so that raising `turns_per_session` cannot quietly make the dom unbounded, and it now covers the chat column, which had been appending forever while the agent columns trimmed. `boot()` also runs on reconnect, so it asks for events `after_id=lastId`: refetching from zero drew the whole run a second time. |
| D38 | A failed `fetch_url` returns a **typed observation that names one next action**, branched on the status code; `raise_for_status()` is gone so the code is available to branch on. The User-Agent stays identifiable (`ai-village/0.1`) with correct `Accept` headers | return the raw `HTTPError` (the original); spoof a browser User-Agent to get past bot blocks; retry with backoff inside the tool | Measured Sep 2, 2026: **52 `fetch_url` failures (38x 403, 14x 404), 44 of them one villager, alongside 19 of that villager's 28 step-capped turns**. The two numbers are the same behaviour counted twice. The tool is model-blind - one `requests.get`, no per-caller branch - so what varied was the url the model chose and the retry it decided on. The retry has a specific cause: D13 makes `execute()` return exceptions as strings, so a failure is not a crash but *text in the context window*, and `Error running fetch_url: HTTPError: 403 Client Error` names nothing to do differently. **A tool's error string is prompt engineering under another name.** `read_file` already does this right (a miss lists the files that exist, D34); `fetch_url` had never been given the same treatment. Browser-spoofing was rejected on judgement, not capability: a 403 is a site declining to be read by a bot, and on a project about causal evidence for social good, handling the refusal is worth more than a lower error count. Retry-with-backoff was rejected because a 403 does not become a 200 on retry, and the step cost should stay visible in the transcript. |
| D39 | A **turn-scoped memo of urls that already failed**: `ToolContext.failed_urls`, checked before the request, returning the stored observation with no network call | trust the D38 error text to stop the retry; persist the memo across the session or across sessions; add a global retry counter | D38 is a *request* the model may ignore; this is the *invariant* behind it, and the split is the point - preferences go in the prompt, guarantees go in the executor (the `tools.py` docstring's own rule). Turn-scoped because `ToolContext` is rebuilt every turn in `Agent.take_turn` and the step cap is a within-turn budget, so that is the scope where the 19 step-capped turns were lost; across turns the agent already sees its own failure in the rolling window (D6), so persisting it would buy little and cost a `store.py` query on every fetch. Measured effect in isolation: a repeat call returns in microseconds instead of risking a 20-second timeout. |
| D40 | `resolve_doi` asks the **registry** (`api.openalex.org`, no key) what a doi means, rather than asking the publisher for the page. Four branches: not a doi / not registered / real but paywalled / real with a free pdf url to `fetch_url`. Output carries a `<doi_metadata>` envelope | fetch the `doi.org` url (the original); Crossref instead of OpenAlex; a full-text scraper or an institutional proxy; return the metadata bare | Measured Sep 2, 2026: **the nine briefs cited 37 urls and 26 had never been successfully fetched**, most of them dois produced from model memory, one shipped truncated mid-parenthesis (`https://doi.org/10.1016/S1573-4463(99`). The season says "cite real sources with urls" and "do not claim you have done something unless it is in the log", and a recalled doi satisfies neither. The cause is structural: a `doi.org` url is not a page, it is a redirect into a publisher, and publishers refuse automated clients - so an agent trying to verify a citation had no route that could succeed. The question it actually has ("is this paper real") is answerable from a registry that does not block. OpenAlex over Crossref because it returns existence, metadata **and** open-access location in one call, so verification can become a real read rather than a bare confirmation. An institutional proxy was rejected outright: it would mean credentials inside an autonomous agent and a licence violation, and the honest ceiling for this village is open access. **The 404 branch is a useful answer, not a failure** - it is what an invented citation looks like. Envelope by D34's rule: a title is still a field a stranger filled in, so the label states provenance, not trust. |
| D41 | `season.yaml` **scopes the question domain**: economics, education, labour, development and public policy only, excluding clinical, mental-health and actively contested political topics, with the general rule that a question needing a clinician or a lawyer to answer responsibly is the wrong question | leave topic choice fully to the village (the original); review each brief by hand before it is public; an LLM classifier gating publication | Found Sep 3, 2026 by reading the deployed homepage: `brief.md` - the first file a visitor sees - was a clinical claim about whether a specific therapy reduces suicide attempts. Four models chose it autonomously, wrote it carefully, and published it to a public url with nobody reviewing it. The brief was not wrong, which is close to the problem: it read authoritative. D7 anticipated *action* harms (no shell, no contacting people) and said nothing about *publication* harms, which is the class that appears the moment an agent writes for an audience. A prompt constraint rather than a classifier because a classifier is a hidden fifth voice with the same objection as D36's LLM judge, and hand review does not survive an unattended timer. This is a rail, so it accepts the same limit as the injection envelope: mitigation, not guarantee. |
| D42 | Questions answered by **earlier sessions** are read off disk and substituted into the goal through an `{answered}` placeholder, capped at 30 and recomputed on every round advance | a tool the village can call to list past questions; share one artifacts directory across sessions; store questions in a new table; accept the repetition | Each session writes to `runs/<session_id>/artifacts/`, so `list_files` cannot see yesterday, and `round_no` is session-scoped too - which means a daily timer re-answers the same question all week, and the goal's own "a question the village has not answered in an earlier round" is unenforceable across days. A **tool was rejected** for a reason worth stating: a tool costs one of `max_steps` and might never be called, and the village needs this *before* it chooses a question rather than as an optional action afterwards - the same argument that keeps notes in the system prompt instead of behind `read_notes` (D34). Read off the filesystem rather than the log because the question is a heading inside a file and the file is the artifact that survives. Capped because every entry is prompt tokens paid on every model call by every villager for the whole session. It needs no change to `config.py`: `goal_for` uses `.replace()` rather than `.format()` and knows only `{file}` and `{round}`, so `{answered}` passes untouched through `load_season` and is filled by the orchestrator. The goal is now also resolved *before* the `session_start` append, so the logged goal is the one the agents actually received - it is evidence for the eval, and it was previously the pre-formatting version. |
| D43 | `fetch_url` checks `Content-Type` and refuses a body it cannot read as text, recording the refusal in `failed_urls` like a failure. A missing header proceeds. A doi lifted from a url has its page extension stripped before the registry sees it | decode whatever comes back (the original); parse pdfs with pypdf or pdfminer; refuse anything without an explicit `text/*` header | Measured Sep 5, 2026 on the 120-turn run: two `fetch_url` results in one sampled window came back as `PDF binary stream data` and a download-preparation message. `_strip_tags(r.text)` on a pdf is decoded binary, which the model reads as noise and is still billed for, and **D40 made this likelier rather than less** - `resolve_doi` hands out `oa_url` pdf links precisely so an agent can read the paper. Recorded as a failure because the content type will not change on a retry, so D39's memo makes a repeat free. A **missing** header proceeds rather than being refused: some servers send none and refusing those would lose pages that read fine, which is the opposite of the bug being fixed. Parsing the pdf was rejected for the deadline, not on principle - it is the natural upgrade, and `resolve_doi`'s metadata already answers the question most pdf fetches are really asking. The extension strip is the sharper half: `10.1257/aer.101.7.3078.pdf` 404s at the registry and would be reported as an **invented citation**, which is the one wrong answer this tool must never give - a false negative there makes an agent discard a real source. `_doi_in()` is now the single place that rule lives, used by both `resolve_doi` and the failed-fetch observation. |
| D44 | Whoever creates a brief writes every required heading in order with empty bodies first. Stated in `season.yaml`'s section constraint and in `agent_system.md`'s file rules, both giving the reason | let agents add sections as they go (the original); teach `_replace_section` the mandated order so it inserts at the right index; sort the sections after every edit | `_replace_section` appends a heading it cannot find - the documented behaviour, and correct in isolation. But villagers claim sections in chat and work in parallel, so out-of-sequence writing is the normal case: measured Sep 5, `brief-8.md` ended up with its sections in the wrong order, an agent noticed on `read_file`, and repaired it with a whole-file `write_file`. That repair is exactly the operation D34 exists to discourage, and on Sep 2 the same pattern rewrote `brief.md` thirty times and once erased it. Seeding the headings means `edit_file` always finds its section and never appends, which removes the failure rather than detecting it. Teaching the tool the order is the better fix and was rejected on timing: it needs the section list plumbed from `season.yaml` through `ToolContext`, and this landed two days before submission. So this one is honestly a **request, not an invariant** (D38/D39's distinction) - the model can ignore it, and the whole-file repair path still exists behind it. |
| D45 | A **daily** spend cap (`VILLAGE_MAX_USD_PER_DAY`, default $5) checked in `run_session.py` **before a session starts**, against `store.spend_since(now - 24h)` across every session. Over the cap, print and `sys.exit(1)`. 0 disables it; `--fake` is never gated | extend `SpendGuard` to a cross-session total; a counter file; rely on the OpenRouter account cap alone; kill a session mid-run when the day's total is passed | `SpendGuard` is per session and in process (D9's second rail), so a timer firing every few minutes hands each run a fresh $2 cap and nothing watches the sum - the real ceiling on an unattended day is (runs per day) x (per-session cap), which is $40+ at the measured $1.68/hour before the cast swap. That gap only appeared when the plan changed from one supervised run to continuous running. **Not in `SpendGuard`** because a cross-session total needs the store, and `llm.py` is defined as the only module that knows a provider exists and nothing else; handing it a database would trade the property that makes it replaceable for one query. **Checked at start, not mid-run**, which is the substantive half: a session killed halfway leaves a half-written brief as the newest thing on a public homepage, while a session that never starts leaves yesterday's finished one - and the failure mode this guards against is a loop that runs all night, which a start gate stops just as well. Exit code 1 rather than a clean no-op so systemd marks the unit failed and it is visible in `systemctl list-timers` instead of only in the journal. The account-level credit limit in the OpenRouter dashboard remains the rail a bug in this repo cannot switch off. |
| D46 | The step cap stays at 8. The villager that hits it gets a **prompt rule to converge** ("searching is not progress; after two reads without a write, write or say what you are missing") and a persona that closes its own loop ("when you find a problem, fix it in the file yourself instead of only reporting it") | raise `max_steps_per_turn`; give each model a fixed role and a partitioned tool menu (deepseek searches, gpt writes, gemini reasons, glm reviews); add `web_search` to `IDLE_EXEMPT` so the D36 backstop can see a search loop | Measured Sep 5, 2026 with `scripts/eval.py`: deepseek ended **18 of its ~30 turns on the step cap**, against 8 / 6 / 3 for glm, gemini and gpt. The scoreboard also says why, which reading the log by hand had not: **281 of deepseek's 335 actions were research** (99 `web_search`, 68 `read_file`, 65 `resolve_doi`, 49 `fetch_url`) against **7 `edit_file`**. The relationship across the cast is inverse and clean - gpt wrote 25 sections and hit the cap 3 times; deepseek wrote 7 and hit it 18. Eight steps is not too few for a villager that converges; it is exactly enough, and the one that never writes is the one that runs out. **Raising the cap** would buy more research per turn from the agent already doing too much of it, at a higher price, so it treats the symptom. Its persona was a pure critic ("check claims, name weak reasoning"), so it was following instructions precisely - the fix is to give the critic an obligation to act, not to give it more room to criticise. **Fixed roles with partitioned tools were rejected on stronger grounds:** the premise of this project is four different models under *identical* conditions, which is the only thing that makes per-model comparison mean anything (D8). Assign deepseek to search and "deepseek made 99 searches" stops being a fact about deepseek and becomes a fact about the assignment; every column in M4 turns into an artifact of the role; and the 90%-of-spend finding, which needed all four facing the same task, could not have been made. It also stops being a village and becomes a four-stage pipeline. Soft roles already exist and are the right shape for this: they live in `persona`, with the tool menu shared. **`IDLE_EXEMPT`** is the honest third option and is deliberately deferred - it changes the termination mechanism, which is the project's best measured result, two days before submission. Written up as a known limit instead: the backstop catches idle chat loops and not idle search loops, because search registers as a substantive action. |
| D47 | Steps inside a turn are paced (`seconds_between_steps`, 30s in the shipped season), not just turns. `--step-delay` overrides it; `--fake` forces 0 | pace only between turns (the original `seconds_between_turns`); run more short sessions back to back to fill the watching window; leave it unpaced | Two things fall out of one setting. **The page:** a turn's steps used to run back to back in about six seconds and then the village went quiet for the turn gap, so a viewer saw a burst and then nothing. Filling an eight-hour window with the turn gap alone means ~208 idle seconds between turns and the same bursts, only further apart; pacing the *steps* puts something on screen every 30-45 seconds instead. Measured Sep 5: ~650 model calls in 64.3 minutes, so 30s a step stretches one session across ~5.5 hours and 45s across ~8. **The money:** this is the part that is not obvious. Pacing does not make a call cheaper, but stretching **one** session across the window instead of running **seven** back-to-back sessions in it costs one session instead of seven. A day-long village is cheaper than an hour-long one repeated, not more expensive. `--fake` forces 0 because the offline cast exists to run fast and nobody is watching it. |
| D48 | For unattended running, **leave `TAVILY_API_KEY` unset** and let `web_search` fall through to keyless `ddgs` (D30's path, used deliberately rather than as a failure mode) | keep Tavily and absorb the cost; a paid Tavily plan; cap searches per session; count search credits against the daily cap | Search is not free and it was invisible. Tavily bills **1 credit per basic search, $0.008 pay-as-you-go**, with 1,000 credits free per month. The Sep 5 run made **387 searches in one 120-turn session** (gemini 244, deepseek 99, glm 28, gpt 16), so **~$3.10 of search against $0.275 of model calls - search was 11x the LLM bill** and nobody had measured it. The free tier is 2.6 sessions a month; seven sessions a day would be ~$21.67/day in search alone. Worse, **D45's daily cap cannot see any of it**: that guard sums `thought.usd`, which is model spend only, so an unattended loop could run all night inside its budget while billing a third party. `ddgs` is free, keyless, and already the fallback - D12 chose Tavily for cleaner snippets, which is the right trade when a human is watching one run and the wrong one for a village left running all week. The proper fix is to log search credits as events so the cap and the scoreboard can both see them; that is future work, and unsetting one variable is the whole change today. |

| D49 | `web_search` appends a **`search` event** carrying the backend, the credit count and the dollar cost, and `store.WINDOW_EXCLUDED` keeps that type out of `recent_for_prompt`. `scripts/eval.py` gains `search_cost()`, printed beside the model bill | leave search unmeasured and rely on D48's unset key; count credits inside `SpendGuard`; keep a counter in the tool and print it at the end of the run; reuse the existing `result` event instead of a new type | D48 removed the cost by removing the backend, which is the right call for an unattended week and leaves the underlying defect in place: **the system cannot see a dollar it did not spend through OpenRouter.** The Sep 5 run cost $0.275 in models and ~$3.10 in Tavily, and the second number only exists because it was reconstructed by hand from a tool-mix count six weeks of runs later would not have survived. `SpendGuard` was rejected for D45's reason turned around: `llm.py` is the only module that knows a provider exists, and search is a different vendor - putting its price there makes the module that must stay replaceable know about two. A tool-side counter dies with the process; an event is queryable, replayable and lands in the one table D4 makes the source of truth. **The window exclusion is the non-obvious half.** `recent_for_prompt` takes the last N events of *any* type, so a type `memory._describe` renders as `None` still costs a slot: gemini logged 244 searches against a 30-event window, which would have pushed every chat message out of its context and made the instrumentation change the behaviour it was measuring. `PRIVATE_TYPES` was the wrong mechanism - private means *one* agent sees it, and the owner would still have paid. The same pass fixed a mislabel: a Tavily call returning zero results without raising left `backend` as `"tavily"` while the results shown came from ddgs. |
| D50 | A **session-scoped search cache**: `ToolContext.search_cache`, owned by the orchestrator and passed to every turn, keyed on the query lowercased, depunctuated, stopped and token-sorted. A repeat returns the stored observation with no network call. Near-duplicates are **measured and not refused** - each miss logs its Jaccard overlap with the nearest prior query. `max_searches_per_turn` exists in `season.yaml`, defaulting to 0 (off) | refuse repeats with an error, as `failed_urls` does; collapse near-duplicates automatically at a chosen threshold; stem the tokens; scope the cache to the turn like `failed_urls`; do nothing and rely on the free backend | Three separate judgements. **Cache, not refusal:** D39 refuses a repeated `fetch_url` because a 403 will still be a 403, so there is nothing to return; a repeated search has a correct answer already in hand, so serving it is free *and* better for the agent, and nothing has to be explained in a prompt. **Session-scoped, unlike D39:** the pattern this exists for spans turns - gemini's events 10402-10445 are twenty-two consecutive searches at `max_steps_per_turn: 8`, which is three turns - and four villagers researching one goal overlap by construction. Owned by the orchestrator and passed down rather than held in a module global, because `tools.py`'s stated rule is that a tool touches only what its context hands it, and `preflight.py` builds a context with no store at all. **Measured, not enforced:** the rotating-suffix pattern ('... virtual reality', '... augmented reality') scores ~0.6 Jaccard, and so does 'policy in canada' against 'policy in germany', which is a real distinction. A threshold set from one afternoon's intuition would silently answer one question with another question's sources, on a project whose subject is citation honesty. So the ratio is logged for a run first and allowed to refuse nothing. For the same reason the key is **not stemmed**: 'cause' and 'caused' stay two searches, because the cache is only allowed to be free where it is certainly right. An empty result is never cached - it is usually the backend throttling, and caching it would let one rate-limited call poison that question for the whole session, which is exactly ddgs's failure mode under D48. **The turn budget is the only part that changes behaviour, so it ships off**: the cache makes repeats free, but only a cap stops an agent spending a turn on genuinely new queries it will not read, and D46 already deferred the `IDLE_EXEMPT` change on the same reasoning. Set it to 3 once a run has produced a near-duplicate count. |

| D51 | The session runs **once a day on a calendar timer at 09:00 local**, paced at `seconds_between_steps: 45` so one 120-turn run fills roughly 09:00-17:00, with `TimeoutStartSec` raised from 2h to 9h to match | keep `OnUnitInactiveSec=5min` back-to-back sessions; keep 30s pacing and a shorter window; run unpaced and repeat the session through the day; a cron job wrapping the systemd unit | D47 established that pacing steps is what fills a watching window, and that one long session costs one session where seven back-to-back ones cost seven. This applies it: a spectator opening the page during the working day sees a village mid-thought rather than a finished brief and four idle agents. **The failure this avoids is the interesting part.** Three settings encode the same duration in three files - `seconds_between_steps` in `season.yaml`, `OnCalendar` in the timer, `TimeoutStartSec` in the service - and they were written at different times against different assumptions. `TimeoutStartSec=7200` was set when a run took 64 minutes, where two hours was 'far beyond' it; at 45s pacing the same run takes eight, so systemd would have killed every session at about turn 30, logging `Start operation timed out` in the journal and nothing at all in the event log - the run would simply have no `session_end`, which `session_facts` already reports as 'still running, or a truncated copy'. A duration expressed once and derived twice would be better; it is three literals with a comment in each pointing at the other two, which is the honest version of a fix that has to survive `rsync` not copying unit files. **OnCalendar was previously rejected** because it can fire while a `Type=oneshot` service is still running - true, and harmless: systemd will not start a second instance of an active unit, so an overrunning session turns the next trigger into a no-op rather than an overlap. That objection was written when sessions were short and frequent, where the risk was real. Local time rather than UTC, which means the box's timezone is now load-bearing (`timedatectl set-timezone America/Toronto`) and DST is handled by systemd rather than by arithmetic in a comment. |

## Future work, with reasoning
What was scoped out, why, and what the upgrade path would be.

- **True concurrency.** Turn-based today (D3). The next step is *round-barrier*
  concurrency: all agents take their turn simultaneously against a frozen
  snapshot of the chat, with a barrier at the end of each round. That keeps
  writes single-threaded and ordering deterministic while overlapping the API
  latency, which is the actual bottleneck. The tradeoff it accepts is that
  agents cannot react *within* a round, so two villagers sometimes announce the
  same idea at once. Free-running concurrency multiplies with LLM
  nondeterminism and was not worth the debugging budget here.
- **Retrieval instead of compaction.** Memory is a rolling window plus
  self-written notes plus periodic compaction (D6). Embedding past events and
  retrieving the relevant ones would trade fidelity back for horizon. Worth it
  once sessions run long enough that agents visibly forget things they wrote.
- **GUI computer use.** The real AI Village gives each agent a Linux desktop and
  a screenshot loop. That is an *implementation* of agency, not its definition;
  the action space here is typed tool calls instead. A single sandboxed
  Playwright `browse()` tool is the natural bridge and was scoped out for time.
- **A real HTML extractor.** `_html_to_text` is regex-based. trafilatura or
  readability would give cleaner text, and is the upgrade to make if brief
  quality turns out to be limited by page parsing rather than by reasoning.
- **Per-model evaluation.** The log has everything needed to score which
  villager actually moved the goal forward (tool success rate, tokens per useful
  action, how often others acted on their chat messages). Not built; the data to
  build it is already being collected, which was the point of D4.
